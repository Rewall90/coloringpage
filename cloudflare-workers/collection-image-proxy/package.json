{
  "name": "collection-image-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker for serving collection post images with hierarchical SEO-friendly URLs",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}